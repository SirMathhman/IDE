import {component$} from '@builder.io/qwik'

export const App = component$(() => {
    return (
        <div style={{
            padding: "0.5rem"
        }}>
            <span style={{
                "font-family": "Arial",
            }}>
                File
            </span>
        </div>
    )
})
